# Change Log

## 0.0.1

- Use default icons instead of custom icons
- Support for light themes
- By default don't display ticked items
- Use different symbols as todo item prefixes
- Show view title commands only on explorer and markdown-todos views
- Remove file sorting by todo count
- Remove code lens support
- Remove todo modification actions (tick, untick, remove)
- Hide headings and files with no unchecked todos when "Toggle ✓ display" is enabled
- Upgrade libraries and dependencies
- Reduce dependencies: Use custom parser instead of `markdown-dom`